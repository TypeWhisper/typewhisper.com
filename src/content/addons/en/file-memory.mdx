---
slug: file-memory
name: File Memory
author: TypeWhisper
icon: HardDrive
description: Local, offline memory storage using JSON files with text-based search.
categories:
  - memory
platforms:
  - mac
  - windows
source: bundled
sourceUrl: "https://github.com/TypeWhisper/typewhisper-mac/tree/main/TypeWhisperPluginSDK/Plugins/FileMemoryPlugin"
sourceUrls:
  mac: "https://github.com/TypeWhisper/typewhisper-mac/tree/main/TypeWhisperPluginSDK/Plugins/FileMemoryPlugin"
  windows: "https://github.com/TypeWhisper/typewhisper-win/tree/main/plugins/TypeWhisper.Plugin.FileMemory"
---

## About

File Memory stores extracted memory entries in `memories.json` inside the plugin data directory. Storage and text search run locally and need no API key. It is a storage provider, not a complete offline transcription or memory-extraction pipeline.

## Setup

1. Enable **File Memory** in **Settings > Integrations**.
2. Enable TypeWhisper's memory feature and select the LLM provider and model that extract facts from transcriptions.
3. In the File Memory settings, search, edit or delete stored entries. The storage directory is managed by TypeWhisper; this view has no folder picker.
4. Test a transcription containing a lasting fact and check the stored entry after extraction completes.

## Data processing

The extraction LLM receives the transcription text. A cloud LLM therefore sends text to its provider even though File Memory stores the resulting facts locally. Retrieved memories can also become part of later workflow prompts. Use local providers for those steps when the full workflow must remain on the device.

On macOS, extracted entries are sent to every enabled, ready memory storage provider. Review other enabled memory add-ons as well.
