---
import BaseLayout from "@/layouts/BaseLayout.astro";
import { Hero } from "@/components/landing/hero";
import { Features } from "@/components/landing/features";
import { HowItWorks } from "@/components/landing/how-it-works";
import { EngineComparison } from "@/components/landing/engine-comparison";
import { AppleDictationComparison } from "@/components/landing/apple-dictation-comparison";
import { AvailablePlatforms } from "@/components/landing/platforms-coming-soon";
import { DownloadCTA } from "@/components/landing/download-cta";
import { t } from "@/i18n/index";

const locale = "de";
const structuredData = {
  "@context": "https://schema.org",
  "@type": "SoftwareApplication",
  name: "TypeWhisper",
  applicationCategory: "UtilitiesApplication",
  operatingSystem: "macOS, Windows, iOS",
  description: t(locale, "structuredData.description"),
  inLanguage: "de",
  url: "https://www.typewhisper.com",
  offers: { "@type": "Offer", price: "0", priceCurrency: "USD" },
};
---
<BaseLayout
  title={t(locale, "landing.title")}
  description={t(locale, "landing.description")}
  canonicalPath="/de/"
  locale="de"
  structuredData={structuredData}
>
  <Hero locale="de" />
  <HowItWorks locale="de" />
  <Features locale="de" />
  <AppleDictationComparison locale="de" client:load />
  <EngineComparison locale="de" client:load />
  <AvailablePlatforms locale="de" />
  <DownloadCTA locale="de" />
</BaseLayout>
