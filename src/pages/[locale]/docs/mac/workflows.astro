---
import DocsLayout from "@/layouts/DocsLayout.astro";
import DocsMacRules from "@/pages/docs/mac/_rules.tsx";
import { t, type Locale } from "@/i18n/index";
import { canonicalPath, getLocaleStaticPaths } from "@/lib/locale-routes";

interface Props {
  locale: Locale;
}

export function getStaticPaths() {
  return getLocaleStaticPaths();
}

const { locale } = Astro.props;
---
<DocsLayout
  title={t(locale, "docs.mac.rules.meta.title")}
  description={t(locale, "docs.mac.rules.meta.description")}
  canonicalPath={canonicalPath(locale, "/docs/mac/workflows")}
  locale={locale}
  platform="mac"
>
  <DocsMacRules locale={locale} />
</DocsLayout>
