---
import DocsLayout from "@/layouts/DocsLayout.astro";
import DocsMacRules from "@/pages/docs/mac/_rules.tsx";
import { t } from "@/i18n/index";

const locale = "en";
---
<DocsLayout
  title={t(locale, "docs.mac.rules.meta.title")}
  description={t(locale, "docs.mac.rules.meta.description")}
  canonicalPath="/en/docs/mac/rules"
  locale={locale}
  platform="mac"
>
  <DocsMacRules locale={locale} />
</DocsLayout>
